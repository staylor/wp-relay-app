import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 2,
    backgroundColor: '#fff',
    padding: 10,
  },

  title: {
    fontSize: 24,
    fontWeight: 'bold',
    paddingTop: 10,
    paddingBottom: 10,
  },
});
