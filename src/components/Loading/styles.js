import colors from 'styles/colors';

export default {
  loading: {
    background: colors.white,
    minHeight: 400,
    paddingTop: 200,
  },

  activity: {
    background: `${colors.white} url('/icons/icon-activity-indicator.gif') no-repeat 50% 10px`,
    height: 56,
    width: '100%',
  },
};
